<%= form_with(model: jit) do |form| %>
  <% if jit.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(jit.errors.count, "error") %> prohibited this jit from being saved:</h2>

      <ul>
        <% jit.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :content, style: "display: block" %>
    <%= form.text_field :content %>
  </div>

  <div>
    <%= form.label :likes, style: "display: block" %>
    <%= form.number_field :likes %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
